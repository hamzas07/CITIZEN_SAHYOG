import MapView from "../../components/MapView";

const MapPage = () => {
  return (
    <div style={{ height: "100vh", width: "100vw" }}>
      <MapView />
    </div>
  );
};

export default MapPage;
